@model  IEnumerable<TMP.Models.InvoiceTransactionModel>
@if (Model.Count() > 0)
{
    <div class="col-md-12 createusertitle">INSURANCE DETAILS</div>
    <div class="col-md-12" style="height:10px;"></div>
    <div class="tab-pane" id="4a">
        <div class="col-md-12" style="height:10px;"></div>
        <div class="col-md-12">
            <table id="example23" class="display" cellspacing="0" width="91%">
                <thead>
                    <tr class="bgspr">
                        <th>Select</th>
                        <th>Insurance Name</th>
                        <th>Policy Number</th>
                        <th>Insurance Broker</th>
                        <th>Insured Amount</th>
                        <th>Premium %</th>
                        <th>Premium Amount</th>
                        <th>Comment</th>
                    </tr>
                <tbody>
                    @foreach (var item in Model)
                {
                        <tr>
                            @if (item.IsSelect == 1)
                            {
                                <td><input type="radio" name="radio" value="@item.InvoiceInsID" checked></td>
                            }
                            else
                            {
                                <td><input type="radio" name="radio" value="@item.InvoiceInsID"></td>
                            }

                            <td>@item.InsuranceName</td>
                            <td>@item.PolicyNo</td>
                            <td>@item.InsuranceBrokerName</td>
                            <td>@item.CurrencyCode @string.Format("{0:0,0.00}", item.InsAmt)</td>
                            <td>@string.Format("{0:0,0.00}", item.PremPer)</td>
                            <td>@item.CurrencyCode @string.Format("{0:0,0.00}", item.PremAmt)</td>
                            <td>@item.Comment</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

    </div>
}
<script>
    $(document).ready(function () {
        $('#example23').DataTable({
        });
    });
</script>

<style>
    .dataTables_filter {
        display: none;
    }
</style>




